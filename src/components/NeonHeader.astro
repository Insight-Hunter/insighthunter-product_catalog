---
const { text } = Astro.props;
---

<h2 class="neon-header">{text}</h2>

<style>
.neon-header {
  font-size: 2.2rem;
  margin: 2rem 0 1rem;
  color: var(--neon-cyan);
  text-shadow: var(--glow-soft);
  position: relative;
  letter-spacing: 0.5px;
}

.neon-header::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: -6px;
  width: 100%;
  height: 3px;
  background: linear-gradient(
    90deg,
    var(--neon-cyan),
    var(--neon-purple),
    var(--neon-blue)
  );
  animation: neon-underline 3s linear infinite;
  border-radius: 2px;
}

@keyframes neon-underline {
  0% { filter: hue-rotate(0deg) brightness(1); }
  50% { filter: hue-rotate(180deg) brightness(1.4); }
  100% { filter: hue-rotate(360deg) brightness(1); }
}
</style>
